version: 2

project_name: selops

before:
  hooks:
    - go mod tidy

builds:
  - main: ./cmd/selops
    binary: selops
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{.Version}}

archives:
  - formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"

brews:
  - repository:
      owner: Gabrielvilabracho
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    directory: Formula
    name: selops
    homepage: "https://github.com/Gabrielvilabracho/selops-ai"
    description: "SelOps — Business operations AI assistant."
    license: "MIT"
    commit_msg_template: "chore: update selops formula to {{ .Tag }}"

scoops:
  - repository:
      owner: Gabrielvilabracho
      name: scoop-bucket
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    name: selops
    homepage: "https://github.com/Gabrielvilabracho/selops-ai"
    description: "SelOps — Business operations AI assistant."
    license: MIT
    commit_msg_template: "chore: update selops to {{ .Tag }}"
